This is the Imdb test application repository

Technology/tool stack: java, Selenide, TestNG, Allure Report

commands to build and run the project:

./gradlew clean build

./gradlew test

commands to generate the allure report:

./gradlew allureReport

./gradlew allureServe
